- name : Variables preference
  hosts: frontend
  vars:
    full_name: "Amar from Play"
  vars_files:
  - course.yaml
  vars_prompt:
  - name: full_name
    prompt: Enter full name
    private: false
  tasks:
  - name : print vars preference
    vars:
      full_name: "amar from task level"
    debug:
        msg: "Hi, {{ full_name }}"
