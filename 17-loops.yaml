- name: Install packages
  hosts: frontend
  tasks:
  - name: Install packages
    package:
      name: "{{ item }}"
      state: present
    loop:
    - ngnix
    - mysql
    - mongodb-mongosh
    - git
