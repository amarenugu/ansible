- name: Install packages
  hosts: frontend
  become: yes
  tasks:
  - name: Install packages
    package:
      name: "{{ item }}"
      state: present
    loop:
    - nginx
    - mysql
    - mongodb
    - git
